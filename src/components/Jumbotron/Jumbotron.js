import React from "react";
import "./Jumbotron.css";

const Jumbotron = () => (
    <header className = "header">
        <h1>Memory Game</h1>
        <h2>Click on an image to earn points, but only click each image once!</h2>
        <h2>Click all 12 to win!</h2>
    </header>
);

export default Jumbotron;